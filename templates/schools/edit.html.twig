{% extends 'base.html.twig' %}

{% block title %}Edit Schools{% endblock %}

{% block body %}
    <h1>Edit Schools</h1>

    {{ include('schools/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_schools_index') }}">back to list</a>

    {{ include('schools/_delete_form.html.twig') }}
{% endblock %}
